# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# client registrations (marketing leads) & retreat bookings
/data/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (secrets stay local / in host dashboard)
.env*
!.env.example
.env.vercel.local

# local performance audits
/lighthouse-reports/

# --- Local-only documentation ---
# This repo is PUBLIC on GitHub. Everything below is either a map of the site's
# weak points or private business material, and must not be published while that
# is true. Delete this whole block once the repo is private — the documentation
# is meant to live in the repo, it just cannot live in a public one.
/docs/history/
/docs/audit/
/docs/journal/
/docs/research/
/docs/ROADMAP.md
/docs/SECURITY-CHECKLIST.md
/docs/QUESTIONS-FOR-JOHARI.md
/docs/OPEN-THREADS.md

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
